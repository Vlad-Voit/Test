// Generated by Selenium IDE
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
public class ByndyusoftTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    driver = new ChromeDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  public String waitForWindow(int timeout) {
    try {
      Thread.sleep(timeout);
    } catch (InterruptedException e) {
      e.printStackTrace();
    }
    Set<String> whNow = driver.getWindowHandles();
    Set<String> whThen = (Set<String>) vars.get("window_handles");
    if (whNow.size() > whThen.size()) {
      whNow.removeAll(whThen);
    }
    return whNow.iterator().next();
  }
  @Test
  public void byndyusoft() {
    // Test name: Byndyusoft
    // Step # | name | target | value | comment
    // 1 | open | https://www.google.ru/ |  | 
    driver.get("https://www.google.ru/");
    // 3 | click | name=q |  | 
    driver.findElement(By.name("q")).click();
    // 4 | type | name=q | Byndyusoft | 
    driver.findElement(By.name("q")).sendKeys("Byndyusoft");
    // 5 | sendKeys | name=q | ${KEY_ENTER} | 
    driver.findElement(By.name("q")).sendKeys(Keys.ENTER);
    // 6 | click | xpath=//h3 |  | 
    vars.put("window_handles", driver.getWindowHandles());
    // 7 | selectWindow | handle=${win9768} |  | 
    driver.findElement(By.xpath("//h3")).click();
    // 8 | click | css=.btn--lg |  | 
    vars.put("win9768", waitForWindow(2000));
    // 9 | assertText | linkText=8 800 775-15-21 | 8 800 775-15-21 | 
    driver.switchTo().window(vars.get("win9768").toString());
    // 10 | assertText | linkText=sales@byndyusoft.com | sales@byndyusoft.com | 
    driver.findElement(By.cssSelector(".btn--lg")).click();
    assertThat(driver.findElement(By.linkText("8 800 775-15-21")).getText(), is("8 800 775-15-21"));
    assertThat(driver.findElement(By.linkText("sales@byndyusoft.com")).getText(), is("sales@byndyusoft.com"));
  }
}
